URL-Shortener/
│
├── src/
│   ├── controllers/      # Lógica de negocio de cada endpoint
│   │   ├── auth.controller.ts
│   │   ├── url.controller.ts
│   │   └── click.controller.ts
│   ├── models/           # Esquemas y modelos de Mongoose
│   │   ├── user.model.ts
│   │   ├── url.model.ts
│   │   └── click.model.ts
│   ├── routes/           # Definición de rutas/routers
│   │   ├── auth.routes.ts
│   │   ├── url.routes.ts
│   │   └── click.routes.ts
│   ├── middlewares/      # Middlewares personalizados (auth, error handler, etc)
│   │   ├── auth.middleware.ts
│   │   └── error.middleware.ts
│   ├── utils/            # Funciones utilitarias y helpers
│   │   └── slug.util.ts
│   ├── config/           # Configuración (DB, env, etc)
│   │   └── db.ts
│   ├── types/            # Tipos y definiciones TypeScript
│   │   └── index.d.ts
│   ├── app.ts            # Configuración principal de la app (Express/Fastify)
│   └── server.ts         # Punto de entrada (levanta el servidor)
│
├── tests/                # Pruebas unitarias e integración
│   ├── auth.test.ts
│   ├── url.test.ts
│   └── click.test.ts
│
├── .env.example          # Variables de entorno de ejemplo
├── .gitignore
├── docker-compose.yml    # (opcional) Para levantar MongoDB y la app localmente
├── Dockerfile            # (opcional) Para contenerizar la app
├── package.json
├── tsconfig.json
├── jest.config.js        # (si usas Jest para testing)
└── README.md